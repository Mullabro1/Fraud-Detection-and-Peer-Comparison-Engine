services:
  python_app:
    image: python:3.9
    container_name: python_container
    working_dir: /app
    volumes:
      - .:/app
    command: ["sh", "-c", "pip install -r requirements.txt && python serve2.py"]
    environment:
      - PYTHONUNBUFFERED=1
